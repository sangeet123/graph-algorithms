package graph.enums;

public enum NodeState {
  //WHITE indicates node that has not been visited
  //GREY indicates node that has been visited but not explored
  //BLACK indicates node that has been fully explored
  WHITE,GREY,BLACK;
}
